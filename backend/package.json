{
  "name": "backend",
  "version": "1.0.0",
  "description": "dear click web app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:local": "env-cmd -f .env nodemon app",
    "start:production": "env-cmd -f .env.production nodemon app",
    "migrate:dev": "knex migrate:latest --env development",
    "seed:dev": "knex seed:run --env development",
    "migrate:production": "env-cmd -f .env.production knex migrate:latest --env production",
    "seed:production": "env-cmd -f .env.production knex seed:run --env production"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "knex": "^3.1.0",
    "morgan": "^1.10.1",
    "mysql2": "^3.14.3"
  },
  "devDependencies": {
    "env-cmd": "^10.1.0"
  }
}
